# Ansible single playbook for centos7 + gitlab  

$ git clone https://github.com/juffaz/ansiblesp-centos7gitlab.git  

$ cd ansiblesp-centos7gitlab  

$ ansible-playbook -i hosts centos7gitlab-deploy.yml  
